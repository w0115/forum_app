<% provide(:title, 'トピック一覧') %>

<div class="topics">
  <h1>トピック一覧</h1>

  <% @topics.each do |topic| %>
    <% if topic.flag != false %>
      <p><%= link_to topic.title, topic %></p>
    <% end %>
  <% end %>
</div>