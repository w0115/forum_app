<% provide(:title, 'スレッド 新規作成') %>

<div class="topic_new_create">
  <h1>スレッド 新規作成</h1>
</div>
    
<div class="center newtopic-bottom">
  <%= form_for(@topic) do |f| %>
    <%= f.label :title, "タイトル" %>
    <%= f.text_field :title %>
    <%= f.submit "スレッド作成", class: "btn btn-primary" %>
  <% end %>
</div>
    